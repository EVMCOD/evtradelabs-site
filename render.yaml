services:
  - type: web
    name: evtradelabs-site
    runtime: node
    plan: starter
    buildCommand: npm ci && npm run build
    startCommand: npx next start -H 0.0.0.0 -p $PORT
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 22
